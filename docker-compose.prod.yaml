services:
  backend:
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://incidentops-db.c1ycaqkewebw.ap-southeast-2.rds.amazonaws.com:5432/incident_ops
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}

  frontend:
    build:
      args:
        VITE_API_BASE_URL: http://3.26.20.252:8080/api/v1

  postgres:
    profiles:
      - never