spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/incidentops
    username: incidentopsuser
    password: arjun123
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true